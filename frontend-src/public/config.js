/*
 * DC Coms runtime configuration.
 *
 * The defaults use the same hostname that serves
 * the DC Coms web application.
 *
 * Override these values only when Matrix or the
 * service accounts use different names.
 */
window.DC_COMS_CONFIG = {
  // homeserverUrl: 'https://chat.example.com',
  // serverName: 'chat.example.com',
  // reminderBotLocalpart: 'reminderbot',
  // opsBotLocalpart: 'dcops',
}
